import { projectSchema } from "./project";
import { serviceSchema } from "./service";
import { siteSettingsSchema } from "./siteSettings";

export const schemaTypes = [projectSchema, serviceSchema, siteSettingsSchema];
